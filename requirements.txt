# MRI Geometrical Utilities Dependencies
# Core scientific computing
numpy>=1.21.0
scipy>=1.7.0

# DICOM processing
pydicom>=2.3.0

# Visualization
matplotlib>=3.5.0

# Image processing (optional)
scikit-image>=0.19.0

# For sparse matrix operations (used in phase unwrapping)
scipy>=1.7.0  # Already included above, but explicitly for sparse operations

# For advanced interpolation (if needed)
scipy>=1.7.0  # Already included above, but explicitly for interpolation

# Web framework
flask>=2.0.0
gunicorn>=20.1.0

# 3D visualization
plotly>=5.0.0
